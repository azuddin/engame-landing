import { Layout } from "@engame/components";
import { PageLayoutProps } from "@engame/types";

const Conditions: PageLayoutProps = () => {
  return <>TODO:Conditions</>;
};

Conditions.layout = Layout;

export default Conditions;
